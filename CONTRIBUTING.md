# 参与贡献

感谢你对 **AI 矿务日报（ai-daily）** 的关注。这个项目刻意保持「零依赖、可复现、零 LLM」，欢迎以同样的原则贡献。

## 提 issue

- **数据不对**（数字、事件、厂商判定有误）：请贴出相关页面/数据文件链接与你的依据，便于对照快照。
- **站点问题**（显示、交互、样式）：附浏览器与截图。
- **新数据源建议**：请注明来源、许可，以及是否允许自动化抓取——许可不清晰的一律不加。
- 模板见 [`.github/ISSUE_TEMPLATE/`](.github/ISSUE_TEMPLATE/)。

## 提 PR

1. Fork 并新建分支。
2. 改动数据管线（`scripts/`）前，先跑自检：

   ```bash
   node scripts/selftest.js
   ```

   78 项纯函数断言必须全绿。
3. 不要提交 `docs/data/raw/`（抓取原始 payload，已在 `.gitignore` 排除，可重新抓取）。
4. 若改动涉及以下内容，请同步更新对应文档：
   - 新增/调整**数据源或许可** → 更新 `NOTICE.md` 与 README「数据来源」表；
   - 新增**原厂** → 在 `scripts/vendor-registry.js` 登记一行；
   - 调整**目录结构 / 命令** → 更新 README 的目录与运行说明。
5. PR 描述请说明「做了什么、为什么、如何验证」，模板见 [`.github/PULL_REQUEST_TEMPLATE.md`](.github/PULL_REQUEST_TEMPLATE.md)。

## 设计原则（不接受违背这些的改动）

- **零 LLM**：站点文案与数据全部由真实数据生成，不调用任何大模型。
- **可复现、可追溯**：每个数字都能回溯到数据源；`--offline` 模式必须确定性可复算。
- **诚实优先**：宁可不做某个榜（如「最会写代码」），也不合成失真的指标。缺口要在 README「已知缺口」里写明。
- **零依赖**：运行时只依赖 Node 内置能力，不引入 npm 依赖。
- **尊重上游许可**：明确排除禁止再分发/自动化抓取的来源。

## 许可

贡献即表示你同意以 [MIT](LICENSE) 许可发布你的代码与文档改动。
